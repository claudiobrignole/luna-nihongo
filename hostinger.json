{
  "build": {
    "command": "npm ci && npm run build",
    "output": "dist"
  },
  "note": "In hPanel → Git → set Node 20+, output directory dist. Add VITE_FIREBASE_* and GEMINI_API_KEY env vars before build."
}
