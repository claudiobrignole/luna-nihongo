{
  "build": {
    "command": "npm ci && npm run build:hostinger",
    "output": "dist"
  },
  "note": "In hPanel → Git → set Node 22+, output directory dist. Add VITE_FIREBASE_*, GEMINI_API_KEY, and FIREBASE_WEB_API_KEY env vars before build."
}
